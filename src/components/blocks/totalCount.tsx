import React from 'react';

const TotalCount = ({count}) => {
    return (
        <div>
            Total Count:
            <br></br>
            <b>{count}</b>
        </div>
    );
};

export default TotalCount;